import {notFound} from 'next/navigation'; import Link from 'next/link'; import Header from '../../../components/Header'; import Footer from '../../../components/Footer'; import Gallery from '../../../components/Gallery'; import {articles,getArticle} from '../../../lib/articles';
export function generateStaticParams(){return articles.map(a=>({slug:a.slug}))}
export async function generateMetadata({params}){const {slug}=await params;const a=getArticle(slug);return a?{title:a.title,description:a.teaser}:{} }
export default async function Article({params}){const {slug}=await params;const a=getArticle(slug);if(!a)notFound();return <><Header/><article><header className="article-head"><Link href="/geschichten" className="back">← ALLE GESCHICHTEN</Link><p className="eyebrow">{a.tag.toUpperCase()}</p><h1>{a.title.toUpperCase()}</h1><p className="deck">{a.teaser}</p></header>{a.competition&&<div className="match-grid"><div><span>SPIEL</span><strong>{a.shortTitle}</strong></div><div><span>WETTBEWERB</span><strong>{a.competition}</strong></div><div><span>STADION</span><strong>{a.stadium}</strong></div><div><span>ZUSCHAUER</span><strong>{a.attendance}</strong></div><div><span>ERGEBNIS</span><strong>{a.result}</strong></div></div>}<figure className="article-hero"><img src={a.hero} alt={a.heroAlt}/></figure>{a.galleryPlacement==='start'&&<Gallery images={a.gallery}/>}<div className="article-layout"><aside><span>{a.dateDisplay}</span><strong>TANDEMHOPPER</strong></aside><div className="article-body"><p className="lead">{a.lead}</p>{a.paragraphs.map((p,i)=><p key={i}>{p}</p>)}</div></div>{a.galleryPlacement!=='start'&&<Gallery images={a.gallery}/>}</article><Footer/></>}
